import { GroupTasksDirective } from './group-tasks.directive';

describe('GroupCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = GroupTasksDirective;
    expect(directive).toBeTruthy();
  });
});
