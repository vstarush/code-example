import { AddTaskGroupDirective } from './add-task.directive';

describe('AddTaskDirective', () => {
  it('should create an instance', () => {
    const directive = AddTaskGroupDirective;
    expect(directive).toBeTruthy();
  });
});
